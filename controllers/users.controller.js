const User = require("../models/users.model.js");

const register = (req, res) => {
  try {
  } catch (error) {
    console.log(error);
  }
};

const login = (req, res) => {
  try {
  } catch (error) {
    console.log(error);
  }
};

module.exports = {
  register,
  login,
};
